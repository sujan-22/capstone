// A generous hit area for each resize corner. The visible handle is drawn by
// the selection frame in CanvasEditor, above the case template.
const HandleComponent = () => {
    return <span className="block size-5" />;
};

export default HandleComponent;
